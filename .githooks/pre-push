#!/bin/sh

echo "Running maintainer docs checks..."
npm run docs:check
status=$?

if [ $status -ne 0 ]; then
  echo "Docs check failed. Run 'npm run docs:check' locally to see specific error details. Push blocked."
  exit $status
fi

echo "Docs check passed."
exit 0
